package infrastructure

import (
	"testing"
)

func Test_RarbgDB(t *testing.T) {
	//rarbgDB, err := NewRarbgDB("../../assets/rarbg_db.sqlite")
	//require.NoError(t, err)
	//
	//torrents, err := rarbgDB.SelectTorrents(context.Background(), nil, 1)
	//assert.NoError(t, err)
	//assert.Len(t, torrents, 100)
}
